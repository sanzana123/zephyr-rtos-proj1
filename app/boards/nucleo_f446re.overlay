#include <zephyr/dt-bindings/gpio/gpio.h>

/{
    leds {
        compatible = "gpio-leds";

        green_led: led_0 {
            gpios = <&gpioa 5 GPIO_ACTIVE_HIGH>;
            label = "User LED LD2";
        };
    };

    aliases {
        led0 = &green_led;
    };
};